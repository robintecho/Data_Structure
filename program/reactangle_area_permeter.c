#include <stdio.h>
#include <conio.h>

void main()
{    
    int l , b ,a,p;
    printf("Enter the length and breadth:");
    scanf("%d%d",&l,&b);
    a=l*b;
    p=2*(l+b);
    printf("Area of the reactangle:%d",a);
    printf("premeter of reactangle:%d",p);

}
